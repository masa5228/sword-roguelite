name: Supabase keepalive

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -sf "$URL/rest/v1/scores?select=id&limit=1" \
            -H "apikey: $ANON" -H "Authorization: Bearer $ANON" -o /dev/null
        env:
          # anon キーは公開前提の値（rankingConfig.ts にも記載）のため直書きでよい
          URL: https://esblafpnxlfdoqsptrvs.supabase.co
          ANON: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVzYmxhZnBueGxmZG9xc3B0cnZzIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM3MzIzNTYsImV4cCI6MjA5OTMwODM1Nn0.S8xYtvlPGTVbdP13WDT99A6dr2iTBeA-V19_RVsfGTQ
